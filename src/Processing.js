import React from 'react';
import classes from './processing.module.css'

const Processing = (props) => {
    return (
        <div className={classes.modal}>
            <div>HELLLLLOOOOOO</div>
        </div>
    );
};

export default Processing;